{
  "name": "mcp-ema-server",
  "version": "0.1.0",
  "private": true,
  "description": "MCP Authorization Server (RFC 7523 jwt-bearer → access token) + Resource Server (PRM, protected API)",
  "main": "index.js",
  "dependencies": {
    "axios": "^1.13.6",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.2.1",
    "jose": "^5.9.6"
  }
}
